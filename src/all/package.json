{
  "name": "webpack-demo",
  "version": "0.0.1",
  "scripts": {
    "dev": "node ./scripts/clean && webpack",
    "release": "node ./scripts/clean && webpack --env.production"
  },
  "devDependencies": {}
}
