:root {
    --red: #f00;
}

h1 {
    color: var(--red);
}

#app {
    text-align: center;
}

.img {
    width: 32px;
    height: 32px;
    background-image: url(./imgs/small.png);
    background-size: cover;
    margin-bottom: 20px;
}